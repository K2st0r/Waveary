# Active Tasks

This file tracks the current highest-value work so a new Codex session can continue Waveary without reconstructing the active queue from chat history.

Keep this file short.

Update it when:

- the active implementation target changes
- a task is completed
- the next recommended step becomes more specific

## Current Focus

1. Keep extending the first permissioned browser-action delivery path.
   Status: in progress
   Current state: Waveary can already open URLs, read the current page, search visible page text, list clickable items, click matched visible elements, fill visible inputs, fill-then-submit visible inputs, open the first visible result link, and open an explicit visible result index through the same chat-side pending-local-action flow. The managed browser layer now matches visible inputs and textareas by associated label / placeholder / aria text, `/api/browser/fill-text`, `/api/browser/fill-submit`, and `/api/browser/open-result` expose those abilities directly, natural chat requests such as `fill search with Waveary`, `fill search with Waveary and submit`, `open result for Waveary`, and `open second result for Waveary` can become pending browser actions, and approved execution records ground follow-up notes back into persisted chat history. The browser automation layer now also performs real Playwright-side fill and submit instead of DOM-only synthetic submit events, includes a search-intent fallback that can recognize search-box-like inputs such as Google's `textarea[name=q][role=combobox]`, prefers `Bing` over `Google` for default natural-language search-site opens in the local browser flow, can filter visible result links by link text, can open an explicit nth visible result when the user asks for it, and uses one repo-root `.waveary` data directory consistently across source and compiled server runs so managed-browser state does not split across two profile trees.
   Next cut: extend the bounded browser layer into one next concrete interaction such as richer multi-field targeting or explicit link disambiguation beyond simple visible-result indexing, now that nth-result selection is in place.

2. Start the first `waveary-voice` delivery path.
   Status: in progress
   Current state: a dedicated `waveary-voice` workspace now exists, the first browser-native TTS planner is implemented, `waveary-web` exposes `/api/voice/speak`, provider-backed TTS has explicit saved voice config plus quality-oriented presets through `/api/voice/config`, dedicated voice mode can diverge from the chat provider, the dedicated Doubao branch has now been migrated from the older `/api/v1/tts` + `appId / cluster` contract to OpenSpeech v3 `/api/v3/tts/unidirectional` with `x-api-key + resourceId`, the console and routing diagnostics now expose `Resource ID` instead of the obsolete `App ID / Cluster` wording, legacy local Doubao config now auto-normalizes old `/api/v1` base URLs, mistaken `resourceId = apiKey` carryover, older `volc.service_type.10029` saved values, and the old `BV001_streaming` / older Mars default voice before the browser reuses them, the dedicated Doubao defaults are now aligned to the current documented `seed-tts-2.0` resource ID plus `zh_female_gaolengyujie_uranus_bigtts`, and the `@waveary/voice` adapter now parses the real newline-delimited chunked success stream so the live local `/api/voice/speak` route returns provider audio instead of browser fallback. The voice layer now also keeps a separate `doubao-legacy` provider family for older app-style grants such as `multi_female_shuangkuaisisi_moon_bigtts`, using `App ID + Access Token` on `/api/v1/tts` without regressing the newer v3 resource-ID route, and local config normalization now clears the earlier polluted `accessKeyId = voiceId` state when the live speaker-discovery keys were accidentally overwritten by a saved voice ID. The first self-hosted cut exists through a generic local HTTP bridge, normal chat turns now also emit a structured companion delivery hint that the browser planner plus provider-backed TTS paths consume directly for style, pace, closeness, and expressiveness, the local self-hosted path now exposes richer engine-tuning fields, the console now has a dedicated voice workspace for those settings, the chat page voice surface has been reduced back to a compact live-entry summary, the voice console now mirrors the provider onboarding pattern through dedicated voice-provider presets plus real OpenAI-compatible voice-model catalog fetches, the frontend respects per-provider voice discovery boundaries by switching local voice selection into manual input while using discovered model labels for OpenAI-compatible vendors, the dedicated Doubao path now also exposes a curated built-in speaker selector instead of only one default voice, auto-loads that curated catalog as soon as the Doubao preset is selected, and renders it through a searchable picker so users can choose supported speakers immediately instead of first seeing the old generic fallback list. That searchable picker now also lives as a full-width in-panel vertical control instead of a narrow inline pill with a floating popover, so the loaded Doubao voices remain visibly inside the console panel and no longer create awkward rightward layout pressure. The same in-panel searchable-list treatment now also applies after manual voice-catalog fetches on select-mode providers, so a loaded catalog no longer hides all voices behind an easy-to-miss trigger while only the status card says `13 voice options loaded`. The split-Doubao browser pass is now complete too: the live console visibly flips between the v3 `Resource ID` path and the legacy `App ID` path, and the live chat page has been verified with DeepSeek still handling text generation while dedicated `doubao-legacy` voice playback separately hits `/api/voice/speak` and returns provider audio. It can still optionally fetch the live Doubao `ListSpeakers` catalog through separate Volcengine `AccessKey ID + Secret Access Key` signing while preserving the current API-key-based TTS path. The voice console ships a broader vendor preset list plus provider-specific form groups and a live right-side guidance panel, the dedicated console credential area keeps `Voice Key` visibly available, stale preset switching no longer leaks Doubao values into later compatible vendors, and the voice routes plus console surface explicit routing diagnostics. The first provider-backed STT slice is now in place too: `@waveary/voice` exports an OpenAI-compatible transcription adapter, `waveary-web` exposes `/api/voice/transcribe`, the saved voice config now carries `sttModel`, the chat page prefers provider-backed microphone capture plus upload/transcription on compatible routes before falling back to browser speech recognition, that browser-side provider STT path now stops on detected post-speech silence with a max-duration safety cap instead of always cutting off at one fixed short timer, and the first interruption-focused realtime pass is now complete too: re-pressing the main live-chat button while the assistant is planning or speaking now stops the current playback immediately and returns to listening without leaving realtime mode. Resume timing after reply completion and retry-listening is also shorter, and stale playback callbacks are ignored through request-id guarding so older audio events cannot overwrite the newer listening state.
   Next cut: keep the new interruption-safe browser loop and decide whether the next highest-value slice is wider provider-specific STT support such as Doubao/local or a deeper transport upgrade toward truer duplex beyond the current browser-side capture heuristics.

3. Keep polishing the `waveary-web` home / console shell.
   Status: in progress
   Current state: the homepage first screen has been compressed further, the hero now fits more comfortably on common desktop heights, the homepage doodle layer now uses a broader generated black-and-white PNG set including study objects plus stamp / envelope / train-ticket / postcard paper keepsakes, the console now uses a tighter toolbar + workspace strip + compact status strip with denser viewport-based panel heights, the provider/model setup workspace is now pinned back into clear view through a dedicated toolbar shortcut plus a visible provider/model status strip so it does not feel like the model selector disappeared, the console now also has a separate voice workspace instead of burying voice routing inside chat, the chat page now exposes a tighter single-band composer row with settings on the left and live-chat/send actions on the right, `Enter` now sends while `Shift+Enter` keeps multiline input, provider setup no longer disappears behind a false `Provider API route not found.` state when only the voice-config route is missing, saving provider config now also invalidates the cached chat runtime immediately so the same active session cannot keep using an older API key or model after a console change, all five console workspaces now share one matched stage shell with unified panel heights plus inner-panel scrolling instead of changing page species from tab to tab, the top workspace-tab cards are back to the tighter compact height, the lower operational panels themselves are now taller, the dedicated chat page now collapses session/voice/permission state into a compact top status strip while auto-scrolling the message canvas and removing the redundant extra live-chat button from the voice summary, the newest layout-containment pass has now fixed the user's reported three overflow hotspots, the old standalone roadmap view has now been replaced by a stronger homepage-ending `Project Route` section while preserving `#roadmap` as a compatibility anchor, and the runtime workspace now exposes a user-facing `Companion Understanding` panel backed by persisted `IdentitySummary` data that can now also be corrected and saved in place instead of staying read-only.
   Next cut: visually verify the new editable runtime understanding panel in-browser, then decide whether the next highest-value shell step is correction provenance / pinning, or a smaller remembered-name / vibe continuity surface beside it.

4. Continue the `waveary-core` dialogue-quality pass.
   Status: in progress
   Current state: memory recall is now stricter and updates `lastRecalledAt`, user-emotion detection is broader than `joy/sadness/neutral`, companion emotion transitions carry more relationship-aware tone state, scripted/provider guidance separates `new`, `warming`, and `growing`, primary continuity-thread selection plus current-turn focus summarization now live in one shared runtime helper reused by both scripted and real-provider reply paths, the helper ranks recalled memories by latest-turn match instead of blindly taking the first recalled item, weak timeline threads are now handled more conservatively during emotional turns, near-tied memory candidates get a small recency bias toward fresher remembered threads, same-age tied memories now also get a very light source-turn preference toward the more recent user-origin thread, short carry-over user follow-ups such as `still scared about that` can now blend with the immediately previous user turn, the same continuity path now also handles more oblique emotional carry-over turns such as `that part still hurts` and `I am not over it yet`, it keeps lower-affect referential follow-ups like `It just feels strange now.` anchored to the previous user topic when they are clearly still describing the same unresolved thread, it keeps short inferential carry-over turns like `Maybe that's why I can't settle tonight.` attached to the prior user topic when the user is still drawing a causal line from that unresolved thread, and it now also keeps weaker unsettled inferential drift lines like `Probably why everything feels a bit unsettled tonight.` attached when the user is still describing the emotional aftereffect of that same unresolved topic. A new shared reply-shape layer now classifies practical / ordinary / playful / reconnection / emotional turns so both live-provider prompts and scripted fallback replies default to shorter human texting cadence unless the emotional moment genuinely calls for more. The first reply-realism tightening pass is now complete too: softer support-seeking turns are classified as emotional earlier, ordinary low-intensity turns now usually avoid a trailing question, provider prompt guidance now pushes harder against essayistic or support-agent cadence, and scripted fallback now keeps new-stage naming warmth plus growing-stage continuity while still shortening everyday replies. The latest bounded realism cut now also treats simple status updates like `I'm home now`, `I'm back`, `just finished`, `I'm on my way`, `I arrived`, `just woke up`, `我到了`, `在路上`, and `刚醒` as a dedicated low-stakes subtype, so ordinary check-in texts get one warm acknowledgment instead of a recap or questionnaire, and early-name inference no longer mistakes `home`, `done`, `on`, or `awake` for a remembered user name. The new-stage runtime now also supports natural getting-to-know-you behavior: it can learn what to call the user, accept a user-given companion nickname, discover desired companion vibe through light banter, preserve those early-acquaintance details into memory instead of requiring a preset persona form, recognize plain first-contact greetings as greeting turns, avoid hijacking emotional `today/tonight` lines into deterministic time answers, and now also anchor itself to a markdown-first companion-soul layer that frames relationship stage as quiet internal calibration instead of a visibly scripted ladder. A fresh market benchmark note now also records what current mature companion products consistently do better: layered memory, identity continuity, unified voice/text continuity, visible control surfaces, and stronger safety posture. The newest bounded runtime cut is now complete too: Waveary has a persisted concept-level `IdentitySummary` layer plus `SimpleIdentityEngine`, and the live-provider prompt now receives a stable understanding block for user self-concept, bond themes, recurring needs, emotional patterns, and companion stance instead of relying only on raw recall. That summary layer is now sharper too: ordinary style/cadence preferences stay distinct from vulnerability cues, emotional care inference can distinguish loneliness and overwhelm from generic comfort needs, and relationship-warming summaries retain trust signals such as naming, ritual, and repeated return instead of flattening back into one generic continuity phrase. The newest refinement now also lets fresher loneliness / overwhelm / anxiety signals suppress older generic comfort wording when the same summary needs conflict.
   Next cut: continue the realism pass by extending short-form everyday cadence beyond status updates and tiny confirmations into the next bounded low-stakes message buckets such as softer `okay then`, `好喔`, `行呀`, or lightly hedged micro-updates, while preserving emotional-first handling and keeping identity inference narrow.

5. Keep permissioned local-time awareness and local execution boundaries bounded and trustworthy.
   Status: in progress
   Current state: chat turns, proactive notification lead copy, and proactive console summary all respond to local daypart only when `timeAwareness` is allowed; direct time/date/day questions now short-circuit deterministically in `waveary-core` before provider generation so real providers cannot ignore the supplied local clock context; that deterministic detector now also catches more indirect Chinese complaint-style phrasings about still not knowing the exact time while no longer misclassifying ordinary emotional turns like `我今天有些不开心`; `localActions` now has its first real ask-first path through chat-side pending action cards plus permission-gated local execution for simple open-url / open-folder / launch-app requests; `full-access` now auto-executes those detected local actions directly from the chat page; same-turn `/api/chat/turn` replies are now kept consistent with that execution path instead of returning a contradictory "I cannot open apps" provider answer; browser open-url actions now run inside a Waveary-managed Playwright persistent browser context; the managed browser layer already exposes bounded current-page info, visible-text extraction, page-text search, clickable-element listing, click-by-text, fill-by-text, and fill-then-submit routes; those browser capabilities now also flow through the same chat-side pending-action card plus `full-access` auto-run path instead of living only as standalone API routes; and page-search intent detection now resolves before the narrower Bilibili follow-up branch so current-page search requests do not accidentally reach for managed browser page state.
   Next cut: verify the live `#chat` page against the new managed browser result-opening path end-to-end, then extend that same bounded browser layer into one next interaction such as richer form-field targeting or explicit nth-result selection instead of jumping to a broad free-form web agent.

6. Preserve continuity discipline under high-frequency iteration.
   Status: ongoing
   Current state: each meaningful step is being verified, committed, pushed, and written back into continuity files, the repository now includes a narrow changed-files mojibake guard at `npm run check:mojibake` for Chinese-facing copy edits on this Windows / PowerShell setup, and there is now a root `npm run reset:test-memory` command that clears local chat/test-session memory through the running local API when available while preserving saved provider and voice config.
   Next cut: keep using the post-commit test-memory reset flow before live verification, keep repository-side state files aligned so session resets do not cause duplicate work or architectural drift, keep using the mojibake guard plus `git diff` for Chinese-copy edits, record any verification caveats like the current Windows `@waveary/core` dist-lock behavior, and preserve the doodle-generation constraints that avoid repeated `524` timeouts.

## Deferred But Important

1. Strengthen live-provider dialogue regression beyond scripted providers.
   Reason: the current quality pass now includes a shared runtime continuity-thread helper, but real OpenAI-compatible behavior still needs more direct coverage for continuity-thread choice and relationship-distance behavior across multiple turns.

2. Decide whether to harden `@waveary/core` test execution on Windows.
   Reason: the current package test script depends on compiled `dist` output and can be misleading unless build and test are run in the right order.

3. Continue future presence-aware work only behind explicit permission boundaries.
   Reason: the product direction allows richer presence later, but current trust boundaries must stay narrow and legible.

4. Keep proactive scheduling explicit and legible.
   Reason: the new local loop is intentionally visible, tab-bound, and user-controlled; future scheduling work should not regress into hidden background behavior.
