{
  "name": "@waveary/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core runtime for Project Waveary.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "openai": "^6.7.0"
  },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "node --test dist/adapters/*.test.js dist/runtime/*.test.js dist/storage/*.test.js"
  }
}
